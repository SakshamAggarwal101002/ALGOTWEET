{
  "version": 2,
  "builds": [
    { "src": "tweetapp/wsgi.py", "use": "@vercel/python","config": {
        "maxLambdaSize": "50mb"
      } }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "tweetapp/wsgi.py" }
  ]
}
